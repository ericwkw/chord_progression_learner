# Google Gemini API key — powers the "AI Analyst" progression feedback panel.
# Get one at https://aistudio.google.com/apikey
# Copy this file to `.env.local` and fill in the value. The app runs fine
# without it; only the AI Analyst panel is disabled.
GEMINI_API_KEY=
